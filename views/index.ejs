<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Health Clinic App</title>
  <link rel="stylesheet" href="/main.css">
</head>
<body>

<header>
  <h1>Health Clinic Booking</h1>
  <nav>
    <a href="/">Home</a>
    <a href="/about">About</a>

    <% if (isLoggedIn) { %>
      <a href="/bookings/add">Add Booking</a>
      <a href="/bookings/list">My Appointments</a>
      <a href="/bookings/search">Search</a>
      <a href="/logout">Logout</a>
    <% } else { %>
      <a href="/users/login">Login</a>
      <a href="/users/register">Register</a>
    <% } %>
  </nav>
</header>

<div class="container">
  <h2>Welcome. Thanks for using our system!</h2>
  <p>To view your appointments please log in or register to Sara's health clinic.</p>

  <% if (isLoggedIn) { %>
    <p>You are logged in as <strong><%= currentUser %></strong>.</p>
  <% } %>
</div>

</body>
</html>