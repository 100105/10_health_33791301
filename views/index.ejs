<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Health Clinic App</title>
  <link rel="stylesheet" href="<%= basePath %>/main.css">
</head>
<body>
<header>
  <h1>Health Clinic Booking</h1>
  <nav>
    <a href="<%= basePath %>/">Home</a>
    <a href="<%= basePath %>/about">About</a>

    <% if (isLoggedIn) { %>
      <a href="<%= basePath %>/bookings/search">Search</a>
      <a href="<%= basePath %>/bookings/add">Add Booking</a>
      <a href="<%= basePath %>/bookings/list">View Bookings</a>
      <a href="<%= basePath %>/users/audit">Audit</a>
      <a href="<%= basePath %>/logout">Logout</a>
    <% } else { %>
      <a href="<%= basePath %>/users/login">Login</a>
      <a href="<%= basePath %>/users/register">Register</a>
    <% } %>
  </nav>
</header>

<div class="container">
  <h2>Welcome</h2>
  <p>This system allows users to register/login and book clinic appointments.</p>

  <% if (isLoggedIn) { %>
    <p>You are logged in as <strong><%= currentUser %></strong>.</p>
  <% } %>
</div>
</body>
</html>
