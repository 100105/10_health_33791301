<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Search Results</title>
  <link rel="stylesheet" href="/main.css">
</head>
<body>

<header>
  <h1>Appointments on <%= date %></h1>
  <nav>
    <a href="/">Home</a>
    <a href="/bookings/search">Search Again</a>
    <a href="/bookings/list">My Appointments</a>
    <a href="/logout">Logout</a>
  </nav>
</header>

<div class="container">
  <% if (!bookings || bookings.length === 0) { %>
    <p><strong>No appointments on this date.</strong></p>
  <% } else { %>
    <table>
      <tr>
        <th>Patient</th>
        <th>Date</th>
        <th>Time</th>
        <th>Reason</th>
        <th>Action</th>
      </tr>

      <% bookings.forEach(b => { %>
        <tr>
          <td><%= b.patient_name %></td>
          <td><%= b.appointment_date %></td>
          <td><%= b.appointment_time %></td>
          <td><%= b.reason %></td>
          <td>
            <form method="POST" action="/bookings/delete/<%= b.id %>">
              <input type="submit" value="Delete">
            </form>
          </td>
        </tr>
      <% }) %>
    </table>
  <% } %>
</div>

</body>
</html>