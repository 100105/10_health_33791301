<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <link rel="stylesheet" href="<%= basePath %>/main.css">
</head>
<body>
<header>
  <h1><%= title %></h1>
  <nav>
    <a href="<%= basePath %>/">Home</a>
    <a href="<%= basePath %>/bookings/add">Add Booking</a>
    <a href="<%= basePath %>/bookings/search">Search</a>
    <a href="<%= basePath %>/logout">Logout</a>
  </nav>
</header>

<div class="container">
  <% if (!bookings || bookings.length === 0) { %>
    <p>No bookings found.</p>
  <% } else { %>
    <table>
      <tr>
        <th>ID</th>
        <th>Patient</th>
        <th>Date</th>
        <th>Time</th>
        <th>Reason</th>
        <th>Action</th>
      </tr>

      <% bookings.forEach(b => { %>
        <tr>
          <td><%= b.id %></td>
          <td><%= b.patient_name %></td>
          <td><%= b.appointment_date %></td>
          <td><%= b.appointment_time %></td>
          <td><%= b.reason %></td>
          <td>
            <form method="POST" action="<%= basePath %>/bookings/delete/<%= b.id %>">
              <input type="submit" value="Delete">
            </form>
          </td>
        </tr>
      <% }) %>
    </table>
  <% } %>
</div>
</body>
</html>